"""
Write a function called `countdown`. It should accept a single
integer argument (assume positive). It should print out that number,
and every number that comes before it, until it reaches 0.

e.g.:
  >>> countdown(3)
  3
  2
  1
  0

"""
